{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for inmem-memo app with tray and global shortcut",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
